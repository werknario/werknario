{
  "name": "werknario-webide-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Web-native KI agent for the GitLab Web IDE: reads the document substrate, proposes edits as a diff, opens a merge request. No terminal. CI is its hand.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test --workspace packages/shared --workspace packages/gitlab-client --workspace packages/proxy",
    "clean": "rm -rf packages/*/dist packages/*/out node_modules/.cache"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
