**What this changes**
A short description of the change and why.

**How it was tested**
- [ ] `npm test` passes
- [ ] `npm run typecheck` is clean
- [ ] `npm run build` succeeds
- [ ] For a new behaviour: a test that would fail without this change

**Notes**
- New code follows the repo's test-first style (tests live in each package's `test/`).
- If this changes what runs vs what is planned, the status table in
  `docs/architecture-and-status.md` was updated.
